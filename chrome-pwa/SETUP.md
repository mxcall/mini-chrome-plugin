# PWA 快速设置指南

## 📋 设置步骤

### 第一步: 生成应用图标

PWA 需要两个尺寸的图标文件。有以下几种方式生成:

#### 方式一: 使用浏览器生成 (推荐)

1. 用浏览器打开 `generate-icons.html`
2. 点击 "下载 192x192" 按钮,保存为 `icon-192.png`
3. 点击 "下载 512x512" 按钮,保存为 `icon-512.png`
4. 将两个文件放在 `chrome-pwa` 目录下

#### 方式二: 使用 Python 脚本生成

```bash
# 需要先安装 Pillow
pip install Pillow

# 运行生成脚本
cd chrome-pwa
python generate_icons.py
```

#### 方式三: 使用在线工具

访问 https://realfavicongenerator.net/ 或其他 PWA 图标生成工具,上传你的设计图,下载生成的图标。

#### 方式四: 使用设计软件

使用 Photoshop、Figma、Sketch 等设计工具创建:
- `icon-192.png` - 192x192 像素
- `icon-512.png` - 512x512 像素

### 第二步: 启动后端服务

在项目根目录运行:

```bash
# Windows
start_server.bat

# 或直接运行
python main.py
```

后端服务将在 `http://localhost:19666` 启动。

### 第三步: 启动 PWA 应用

#### 方式一: 使用批处理脚本 (Windows)

```bash
cd chrome-pwa
start-pwa.bat
```

应用将在 `http://localhost:8080` 启动。

#### 方式二: 手动启动

```bash
cd chrome-pwa

# Python
python -m http.server 8080

# Node.js (需要先安装 http-server)
npx http-server -p 8080
```

### 第四步: 在浏览器中访问

1. 打开 Chrome 或 Edge 浏览器
2. 访问 `http://localhost:8080`
3. 查看应用是否正常运行

### 第五步: 安装 PWA 到桌面

1. 在浏览器地址栏右侧,点击 "安装" 图标 (➕)
2. 或者等待页面底部弹出安装提示
3. 点击 "安装" 按钮
4. 应用将被添加到桌面和开始菜单

## ✅ 验证清单

安装前请确认:

- [ ] 图标文件已生成 (icon-192.png, icon-512.png)
- [ ] 后端服务正在运行 (localhost:19666)
- [ ] PWA 通过 HTTP 服务器访问 (不是 file:// 协议)
- [ ] 使用支持 PWA 的浏览器 (Chrome/Edge)
- [ ] 打开浏览器控制台,检查是否有错误

## 🔍 常见问题

### Q1: 看不到安装按钮?

**原因**: 
- 使用了 file:// 协议打开 HTML
- 图标文件缺失
- Service Worker 注册失败

**解决方案**:
- 必须通过 HTTP(S) 服务器访问
- 检查图标文件是否存在
- 打开 DevTools > Application > Manifest 检查配置

### Q2: Service Worker 注册失败?

**原因**: 
- 文件路径错误
- CORS 问题

**解决方案**:
- 确保所有文件在同一目录
- 使用本地 HTTP 服务器,不要跨域访问

### Q3: 后端连接失败?

**原因**: 
- 后端服务未启动
- 端口被占用

**解决方案**:
- 启动 main.py
- 检查 19666 端口是否可用

### Q4: 图标显示异常?

**原因**: 
- 图标文件格式错误
- 文件路径错误

**解决方案**:
- 确保图标是 PNG 格式
- 检查 manifest.json 中的路径配置

## 📱 使用建议

1. **桌面使用**: 安装 PWA 后,从桌面快捷方式启动,获得类原生应用体验
2. **移动设备**: 在手机浏览器中访问,添加到主屏幕
3. **离线访问**: 首次加载后,即使断网也可以访问应用界面
4. **快捷键**: 使用 Ctrl+Enter 快速上传文本

## 🚀 下一步

PWA 安装成功后:

1. 从桌面图标启动应用
2. 上传文件测试功能
3. 尝试离线访问 (断网后重新打开)
4. 查看 DevTools > Application 了解 PWA 状态

## 📞 技术支持

如遇到问题:

1. 查看浏览器控制台错误信息
2. 检查 DevTools > Application > Service Workers
3. 验证 DevTools > Application > Manifest
4. 清除缓存后重试

---

**祝使用愉快! 🎉**
